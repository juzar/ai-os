# AI-OS COMPLETE

## Setup
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
pip install anthropic==0.34.0 httpx==0.27.0 requests

Create .env:
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GITHUB_TOKEN=

Run:
ai.bat devops "test"
